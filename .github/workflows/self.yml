name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted   # 👈 changed here

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run one-line script
        run: echo "Hello, world!"

      - name: Run multi-line script
        run: |
          echo "Add other actions to build,"
          echo "test, and deploy your project."
